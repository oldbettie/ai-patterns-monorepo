{
  "name": "@proxy-fam/cloudflare",
  "version": "1.0.0",
  "description": "Cloudflare Workers for family proxy service",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "echo 'Cloudflare Worker build - skipping for now'",
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "clean": "rm -rf dist/",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@proxy-fam/common": "workspace:*",
    "@proxy-fam/database": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}