services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: quick-pdfs-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: quick-pdfs
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5

  # PostgreSQL Dashboard
  pg-dashboard:
    image: sosedoff/pgweb:latest
    container_name: quick-pdfs-pgweb
    restart: unless-stopped
    ports:
      - '5050:8081'
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: 'postgres://postgres:postgres@postgres:5432/quick-pdfs?sslmode=disable'
    command: pgweb --bind=0.0.0.0 --listen=8081 --url=$DATABASE_URL

volumes:
  postgres_data:
